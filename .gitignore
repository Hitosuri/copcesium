node_modules/
dist/
coverage/
.env
.env.local

# CycloneDX SBOM: CI regenerates it per run and uploads it as a build
# artifact, so the checked-out tree should never carry one.
sbom.cdx.json

# for gitingest
digest.txt

# AI agent instruction files (local-only, not for repo tracking)
AGENTS.md
CLAUDE.md
